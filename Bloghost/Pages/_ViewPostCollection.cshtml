@model List<Bloghost.Domain.Post>

<div class="container-fluid posts-container">
    @foreach (var post in Model)
    {
        { await Html.RenderPartialAsync("_ViewPost.cshtml", post.Id); }
        <br />
    }
</div>